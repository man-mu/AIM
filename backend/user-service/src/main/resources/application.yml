# 兜底配置：Nacos 不可达时的本地 fallback
# 特定环境的配置写在 application-{profile}.yml 中
server:
  port: ${SERVER_PORT:8081}

mybatis-plus:
  configuration:
    map-underscore-to-camel-case: true

aim:
  snowflake:
    worker-id: 0
