/**
 * @deprecated 应用框架已重构为 @/components/layout/AppLayout（导航栏 + 路由 Outlet），
 * 三栏工作台见 @/modules/conversation/ConversationWorkspace。
 * 本文件保留仅为兼容历史引用，可安全删除。（清理脚本见 scripts/cleanup-obsolete.md）
 */
export { AppLayout as default } from '@/components/layout/AppLayout';
